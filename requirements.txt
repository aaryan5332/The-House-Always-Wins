Django>=4.2.0,<5.0.0
# Core Django framework for the web application

# No additional dependencies required for this simple slot machine game
# Django includes everything we need:
# - SQLite database (default)
# - JSON field support
# - UUID field support
# - Session management
# - CSRF protection
# - Static file serving 